{
  "name": "guess-kid",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:web": "if ! command -v flutter >/dev/null 2>&1; then git clone https://github.com/flutter/flutter.git --depth 1 -b stable $HOME/flutter; fi && export PATH=\"$PATH:$HOME/flutter/bin\" && flutter config --enable-web && flutter pub get && flutter build web --release"
  }
}
